
def makeArray(size : number, value) : array {
	var out : array;
	Array.Resize(out, size);
	for (out) {
		out[_index_] = Copy(value);
	}
	return out;
}

object Loc
{
	x = 2;
	y = 0;
}

public def game() {
	
	var test = false;
	var l : Loc;
	var Field : array = makeArray(3, test == true ? 2 : 4);
	
	for (item in Field) println(item);
	
	println(Field);
}