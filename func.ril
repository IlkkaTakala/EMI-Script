extend Script:

object Test
{
    test : string = "value";
    time : number;
    any = "anyval";
}

def func() {
	var test : number = 5;
	var other : Test;
	if (var auto = 23) {
		test = 15;
	}else{
		test = 10;
	}
	test = test + 4 / test * 65 - test;
	return test / 3;
}

public def funct(value, vel, ) {
    
}

# compile ../../ScriptTest.ril -s