
var NumberTest = 10;

var Thread = 0;

public def game() {
	
	var local = Thread++;
	
	var arr = [1, 3, 5, 6];
	
	print(arr[2]);
	arr[2] = [1, 2];
	print(arr[2][1]);
	
	var index = 0;
	for (var i = 0; i < 6; ++i) {
		
		if (i < 3) continue;
		index++;
	}
	println(index);
	
	return;
	
	while (true) {
		println("Thread: ´{local}´, Number: ´{NumberTest++}´");
		delay(500);
	}
	
}