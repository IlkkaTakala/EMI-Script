extend Script:

object Test
{
    test : string = "value";
    time : number;
    any = "anyval";
}

public def func(value : number) {
	var test : number = 5;
	var other : Test;
	test = -100;
	if (true) {
		test += value;
	}
	for (var i = 0; i < 10; i++) {
		++test;
	}
	test = funct(100, test);
	return -test;
}

def funct(value, vel) : number {
    return value + vel;
}

# compile ../../ScriptTest.ril -s